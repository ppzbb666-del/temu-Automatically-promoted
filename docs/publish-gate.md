# 发布闸门

自动化同步到店小秘前，任务必须先通过人工审核。

## 规则

- 控制台“一键同步到店小秘”只允许 `approved` 状态任务。
- 自动化 runner 默认从 `GET /tasks/active?requireApproved=true` 取任务。
- 未审核通过的任务不会被视为当前可发布任务。

## 行为

- 审核通过后，任务才能进入店小秘同步流程。
- 草稿或商品再次编辑后，任务会回到 `reviewing`，需要重新审核。
- 如果接口没有可用任务，runner 和控制台都会返回明确错误，而不是继续执行。
