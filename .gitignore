node_modules
dist
build
.vite
.runtime
output
.DS_Store
*.log

# 本地环境变量(含 LLM_API_KEY 等密钥),绝不入库
.env
.env.*
!.env.example

# 真实页面校准抓下来的已登录浏览器用户目录 —— 含 cookie/凭据,绝不入库
待发布编辑页/

# 本地源码备份(含 .claude/plans 里约定的 *.bak 备份纪律),不入库
# 备份统一放 .backups/(带日期前缀),不要散落在 src 里
*.bak
*.corrupt-backup
.backups/
